{
  "name": "meal-split-web",
  "version": "1.0.0",
  "description": "A bilingual, browser-only tool for splitting shared expenses.",
  "private": false,
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
